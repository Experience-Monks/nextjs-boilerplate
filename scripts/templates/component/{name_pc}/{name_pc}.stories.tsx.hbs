{{#if imperativeHandle}}
import { FC, useRef } from 'react'
{{/if}}
import { StoryFn } from '@storybook/react'

import { {{#if imperativeHandle}}{{name_pc}}Handle, {{/if}}View, ViewProps } from './{{name_pc}}'

export default { title: 'components/{{name_pc}}' }

{{#if imperativeHandle}}
const Template: FC<ViewProps> = ({ ...args }) => {
  const handleRef = useRef<{{name_pc}}Handle>(null)
  return <View {...args} handleRef={handleRef} />
}

{{/if}}
export const Default: StoryFn<ViewProps> = (args) => {
  return <{{#if imperativeHandle}}Template{{else}}View{{/if}} {...args} />
}

Default.args = {}

Default.argTypes = {}
