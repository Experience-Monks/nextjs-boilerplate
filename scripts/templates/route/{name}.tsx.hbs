import type { Page{{name_pc}}Props } from '@/components/Page{{name_pc}}'
import type { GetStaticProps } from 'next'

import { CmsService } from '@/services/cms'

export const getStaticProps: GetStaticProps<Page{{name_pc}}Props> = async () => {
  return {
    props: {
      content: CmsService.getPageContent({{name_cc}})
    }
  }
}

export { Page{{name_pc}} as default } from '@/components/Page{{name_pc}}'
