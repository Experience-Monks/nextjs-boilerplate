import type { StoryFn } from '@storybook/react'
import type { {{#if imperativeHandle}}ViewHandle, {{/if}}ViewProps } from './{{titleCase name}}.view'

{{#if imperativeHandle}}
import { useRef } from 'react'

{{/if}}
import { View } from './{{titleCase name}}.view'

export default { title: 'components/{{titleCase name}}' }

export const Default: StoryFn<ViewProps> = (args) => {
{{#if imperativeHandle}}
  const handleRef = useRef<ViewHandle>(null)
{{/if}}
  return <View {...args} {{#if imperativeHandle}}handleRef={handleRef} {{/if}}/>
}

Default.args = {}

Default.argTypes = {}
