import ResponsiveImage, { Props } from './ResponsiveImage';

export default { title: 'components/ResponsiveImage' };

export const Default = (args: Props) => <ResponsiveImage {...args} />;

Default.args = {
  // add control data
};
