import PlainList, { Props } from './PlainList';

export default { title: 'components/PlainList' };

export const Default = (args: Props) => <PlainList {...args} />;

Default.args = {
  // add control data
};
