import Container, { Props } from './Container';

export default { title: 'components/Container' };

export const Default = (args: Props) => <Container {...args} />;

Default.args = {
  // add control data
};
