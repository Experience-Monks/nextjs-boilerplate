import TestRail, { Props } from './TestRail';

export default { title: 'components/TestRail' };

export const Default = (args: Props) => <TestRail {...args} />;

Default.args = {
  // add control data
};
