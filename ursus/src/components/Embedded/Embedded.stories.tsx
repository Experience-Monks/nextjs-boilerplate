import Embedded, { Props } from './Embedded';

export default { title: 'components/Embedded' };

export const Default = (args: Props) => <Embedded {...args} />;

Default.args = {
  // add control data
};
