import Sidebar, { Props } from './Sidebar';

export default { title: 'components/Sidebar' };

export const Default = (args: Props) => <Sidebar {...args} />;

Default.args = {
  // add control data
};
